@page
@using Microsoft.AspNetCore.Authorization;
@attribute [Authorize(Roles = "Admin")]
@model TimimInnovation.Pages.Admin.Securities.Activity.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>LastActivity</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LastActivity.Username)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LastActivity.Username)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LastActivity.LastSeen)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LastActivity.LastSeen)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.LastActivity?.LastActivityId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
